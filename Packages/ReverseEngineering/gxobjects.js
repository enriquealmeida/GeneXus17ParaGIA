function AttCrossReference(attName)
{
	try
	{ 
		window.external.AttCrossReference(attName);
	}
	catch (exception)
	{}
}

function ApplyWarningFix(fixId)
{
	try
	{ 
		window.external.ApplyWarningFix(fixId);
	}
	catch (exception)
	{}
}

function OpenObjectReport(objPreffix, attName)
{
	try
	{ 
		window.external.OpenObjectReport(objPreffix, attName);
	}
	catch (exception)
	{}
}